# Copy to frontend/.env.production before building the static export.
# The build inlines this value, so it must point at the live backend.
NEXT_PUBLIC_API_URL=https://your-username.pythonanywhere.com/api/v1
